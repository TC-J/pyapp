[project]
name = "pyapp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "TC Jowers", email = "jowerstc@outlook.com" }
]
requires-python = ">=3.14"
dependencies = [
    "platformdirs>=4.11.11",
    "pyyaml>=6.0.3",
    "tomli-w>=1.2.0",
]


[project.scripts]
pyapp = "pyapp:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
